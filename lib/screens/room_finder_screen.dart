import 'package:flutter/material.dart';

class RoomFinderScreen extends StatelessWidget {
  static const name = 'room_finder_screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Room Finder'),
      ),
      body: Center(
        child: Text('coming soon!'),
      ),
    );
  }
}
